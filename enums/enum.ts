
enum VaultTypes {
  AllVault = 'All Vaults',
  MyVault = 'My Vault',
}

enum MenuBarVaultTypes {
  All = 'All',
  Passwords = 'Passwords',
  SecureNotes = 'Secure Notes',
  PaymentCards = 'Payment Cards',
  BankAccounts = 'Bank Accounts',
  Addresses = 'Addresses',
  EnvVariables = 'Environment Variables'
}

export { VaultTypes, MenuBarVaultTypes }
